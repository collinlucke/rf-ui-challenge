# rf-ui-challenge

### To run it:

- Unzip files to directory of your choosing (probably already gone that far if you are reading this)
- At the root of your directory run `pnpm install` or `npm install`
- Then run `pnpm/npm dev`
- App runs on port `5173` so go to `localhost:5173` in your browser
